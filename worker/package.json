{
  "name": "driveguard-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute driveguard --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "bcryptjs": "^3.0.2",
    "jose": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
